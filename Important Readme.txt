IMPORTANT SETUP INFO:

In order for this project to run properly, you need to copy FCC.DLL into EWTKServer\bin.
Visual Studio refuses to include it through normal means because it is not a .NET binary.